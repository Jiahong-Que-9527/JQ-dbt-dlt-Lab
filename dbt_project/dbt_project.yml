name: dlt_dbt_sandbox
version: '1.0.0'
config-version: 2

profile: dlt_dbt_sandbox

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dlt_dbt_sandbox:
    staging:
      +materialized: view
    marts:
      +materialized: table
